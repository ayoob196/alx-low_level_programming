#include<stdio.h>
/**
 * main - i'm tired
 * Return: but i\m strong
 */
int main(void)
{
	int start;

	for (start = 48; start < 58; start++)
	{
		putchar(start);
	}
	for (start = 97; start < 103; start++)
	{
		putchar(start);
	}
	putchar('\n');
	return (0);
}
